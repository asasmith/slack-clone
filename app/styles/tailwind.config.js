module.exports = {
    mode: ['jit'],
    purge: ['../../app/**/*.hbs', '../../app/index.html'],
    darkMode: false, // or 'media' or 'class'
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
};
